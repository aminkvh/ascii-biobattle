name: Release

# Triggers whenever you push a tag like v1.0.0 or v0.3.1
on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    name: Build all platforms & publish release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build Linux amd64
        run: GOOS=linux GOARCH=amd64 go build -o dist/ascii-biobattle_linux_amd64 .

      - name: Build Linux arm64
        run: GOOS=linux GOARCH=arm64 go build -o dist/ascii-biobattle_linux_arm64 .

      - name: Build macOS Intel (amd64)
        run: GOOS=darwin GOARCH=amd64 go build -o dist/ascii-biobattle_mac_amd64 .

      - name: Build macOS Apple Silicon (arm64)
        run: GOOS=darwin GOARCH=arm64 go build -o dist/ascii-biobattle_mac_arm64 .

      - name: Build Windows amd64 (.exe)
        run: GOOS=windows GOARCH=amd64 go build -o dist/ascii-biobattle_windows_amd64.exe .

      - name: Build Windows amd64 (.scr screensaver)
        run: GOOS=windows GOARCH=amd64 go build -o dist/ascii-biobattle_amd64.scr .

      - name: Build Windows arm64 (.scr screensaver)
        run: GOOS=windows GOARCH=arm64 go build -o dist/ascii-biobattle_arm64.scr .

      - name: Create GitHub Release with all binaries
        uses: softprops/action-gh-release@v2
        with:
          name: "ASCII Biobattle ${{ github.ref_name }}"
          body: |
            ## ASCII Biobattle ${{ github.ref_name }}

            A cross-platform terminal screensaver: medieval knights vs AI robots on a procedural battlefield with a starry night sky, animated water, and occasional bio flashes.

            ### Downloads

            | Platform | File |
            |---|---|
            | Linux x86-64 | `ascii-biobattle_linux_amd64` |
            | Linux ARM64 | `ascii-biobattle_linux_arm64` |
            | macOS Intel | `ascii-biobattle_mac_amd64` |
            | macOS Apple Silicon | `ascii-biobattle_mac_arm64` |
            | Windows x86-64 (exe) | `ascii-biobattle_windows_amd64.exe` |
            | Windows x86-64 (screensaver) | `ascii-biobattle_amd64.scr` |
            | Windows ARM64 (screensaver) | `ascii-biobattle_arm64.scr` |

            ### Quick Start
            ```bash
            # Linux / macOS
            chmod +x ascii-biobattle_linux_amd64
            ./ascii-biobattle_linux_amd64

            # With options
            ./ascii-biobattle_linux_amd64 --density 5 --theme night --speed 30
            ```

            See [README.md](README.md) for full setup instructions per platform.
          files: |
            dist/ascii-biobattle_linux_amd64
            dist/ascii-biobattle_linux_arm64
            dist/ascii-biobattle_mac_amd64
            dist/ascii-biobattle_mac_arm64
            dist/ascii-biobattle_windows_amd64.exe
            dist/ascii-biobattle_amd64.scr
            dist/ascii-biobattle_arm64.scr
          draft: false
          prerelease: false
